import React from "react";

type Props = {};

const ClipFooter = (props: Props) => {
  return (
    <div>
      <button>Undo</button>
      <button>Start / Stop</button>
    </div>
  );
};

export default ClipFooter;
